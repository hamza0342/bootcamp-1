import React from 'react';

const Approval= props => 
{
    return (
    <div>
        <div> Hello {props.name}. Your id is {props.id} and your semester is {props.semester}</div>
        
    </div>
    
    );


}

export default Approval;